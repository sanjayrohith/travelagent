## Deployment config placeholder (platform-agnostic)
# This project does not rely on Firebase App Hosting.
# Remove this file or adapt it for your chosen platform.

# Example placeholder keys to avoid breaking tools that expect this file:
app:
  name: wanderlust-packages
  runtime: nodejs
